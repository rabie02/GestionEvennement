<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp"
    android:background="@drawable/background">

    <ImageView
        android:layout_width="258dp"
        android:layout_height="111dp"
        android:layout_marginBottom="32dp"
        android:contentDescription="Logo de l'application"
        android:src="@drawable/im1" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="75dp"
        android:layout_marginBottom="32dp"
        android:textColor="@color/button_background"
        android:text="Bienvenue Dans l'application"
        android:textSize="24sp"
        android:textStyle="bold" />

    <Button
        android:id="@+id/buttonCreateEvent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Créer un événement"
        android:backgroundTint="@color/button_background"
        android:layout_marginBottom="16dp"
        style="@style/CustomButtonStyle"/>

    <Button
        android:id="@+id/buttonViewEvents"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Voir les événements"
        android:backgroundTint="@color/button_background"
        android:layout_marginBottom="16dp"
        style="@style/CustomButtonStyle"/>

    <Button
        android:id="@+id/buttonGuestManagement"
        style="@style/CustomButtonStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:backgroundTint="@color/button_background"
        android:text="Gestion des participants" />

    <Button
        android:id="@+id/buttonBudgetManagement"
        style="@style/CustomButtonStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:backgroundTint="@color/button_background"
        android:text="Gestion du budget" />

    <Button
        android:id="@+id/buttonLogout"
        android:backgroundTint="@color/button_background"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Déconnexion"
        style="@style/CustomButtonStyle"
        />

</LinearLayout>
